{
  "name": "lupo-grigio-admin",
  "version": "1.0.0",
  "description": "Lupo Grigio Admin Panel with Database Integration",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build complete'",
    "migrate": "node scripts/migrate-to-db.js"
  },
  "dependencies": {
    "@vercel/postgres": "^0.5.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
